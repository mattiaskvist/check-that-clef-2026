---
phase: 03-duplicate-disambiguation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clef_retrieval/disambiguation.py
  - src/clef_retrieval/pipeline.py
  - tests/test_disambiguation.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - RNK-03
must_haves:
  truths:
    - "System detects duplicate-title collisions only in top-K reranked results (lazy evaluation)."
    - "System uses normalized title matching to group duplicates (handles casing, punctuation, unicode)."
    - "System prioritizes disambiguation signals: Author > Method/Finding > Venue/Year."
    - "System preserves semantic reranker order when disambiguation signals tie."
  artifacts:
    - path: "src/clef_retrieval/disambiguation.py"
      provides: "Title normalization, collision detection, and multi-signal disambiguation scoring"
    - path: "src/clef_retrieval/pipeline.py"
      provides: "Disambiguation integration after semantic reranking"
    - path: "tests/test_disambiguation.py"
      provides: "Unit tests for collision detection and signal priority"
  key_links:
    - from: "src/clef_retrieval/pipeline.py"
      to: "src/clef_retrieval/disambiguation.py"
      via: "disambiguate_title_collisions() call after semantic reranking"
      pattern: "disambiguate|collision|duplicate"
    - from: "src/clef_retrieval/disambiguation.py"
      to: "src/clef_retrieval/query_policy.py"
      via: "reuse normalize_tokens_for_language() for signal matching"
      pattern: "normalize_tokens_for_language"
---

<objective>
Implement duplicate-title disambiguation with lazy collision detection and multi-signal ranking.

Purpose: Satisfy RNK-03 by correctly ranking papers with identical titles using Author > Method/Finding > Venue/Year priority.
Output: Disambiguation module, pipeline integration, and comprehensive test coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-duplicate-disambiguation/03-CONTEXT.md
@.planning/phases/03-duplicate-disambiguation/03-RESEARCH.md
@src/clef_retrieval/pipeline.py
@src/clef_retrieval/schemas.py
@src/clef_retrieval/query_policy.py
@src/clef_retrieval/config.py

<interfaces>
From src/clef_retrieval/schemas.py:
```python
class TweetEvidence(BaseModel):
    candidate_authors: list[str]
    method_terms: list[str]
    finding_terms: list[str]
    time_or_venue_hints: list[str]

class PaperEvidence(BaseModel):
    pubkey: str
    title: str
    authors: str
    venue: str
```

From src/clef_retrieval/pipeline.py:
```python
def rank_from_query_embedding(...) -> list[str]:
    # After semantic reranking (line ~335)
    # Insert disambiguation here before final top-5 clipping
```

From src/clef_retrieval/config.py:
```python
class RetrievalConfig(BaseModel):
    weight_title_author: float = 5.0
    weight_method_finding: float = 3.0
    weight_keywords: float = 1.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for collision detection and disambiguation (D-01 through D-06)</name>
  <read_first>
    - .planning/phases/03-duplicate-disambiguation/03-CONTEXT.md
    - .planning/phases/03-duplicate-disambiguation/03-RESEARCH.md
    - src/clef_retrieval/schemas.py
  </read_first>
  <files>tests/test_disambiguation.py</files>
  <action>Create failing tests that lock: (a) collision detection only activates when duplicate normalized titles exist in candidates per D-01/D-02, (b) author signal has highest disambiguation weight per D-03, (c) method/finding terms have secondary weight, (d) venue/year hints have lowest weight, and (e) semantic reranker order is preserved when disambiguation scores tie per D-05/D-06. Include fixtures with duplicate-title candidate metadata.</action>
  <acceptance_criteria>New tests fail due to missing disambiguation module, proving coverage for RNK-03 behavior before implementation.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_disambiguation.py -x</automated>
  </verify>
  <done>Phase-3 disambiguation contracts are explicit and executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement disambiguation module with title normalization and multi-signal scoring (D-01 through D-04)</name>
  <read_first>
    - .planning/phases/03-duplicate-disambiguation/03-RESEARCH.md
    - src/clef_retrieval/schemas.py
    - src/clef_retrieval/query_policy.py
    - src/clef_retrieval/config.py
  </read_first>
  <files>src/clef_retrieval/disambiguation.py</files>
  <action>Create disambiguation.py with: (1) normalize_title() using unicodedata.normalize("NFKC") and punctuation stripping per research patterns, (2) detect_title_collisions() that groups candidates by normalized title and returns only collision groups (>1 member) per D-01, (3) compute_author_similarity() using difflib.SequenceMatcher with substring fast-path per research, (4) compute_method_match() and compute_venue_match() helpers, (5) compute_disambiguation_score() that weights signals per D-03 using config.weight_title_author/weight_method_finding/weight_keywords, and (6) disambiguate_title_collisions() that applies scoring only to collision groups and preserves semantic order for ties per D-05/D-06.</action>
  <acceptance_criteria>Disambiguation module exposes clean API for collision detection and scoring with deterministic behavior.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_disambiguation.py -x</automated>
  </verify>
  <done>RNK-03 disambiguation logic is implemented and test-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate disambiguation into pipeline after semantic reranking (D-05, D-06)</name>
  <read_first>
    - src/clef_retrieval/pipeline.py
    - src/clef_retrieval/disambiguation.py
    - .planning/phases/02-semantic-reranking/02-VERIFICATION.md
  </read_first>
  <files>src/clef_retrieval/pipeline.py, tests/test_pipeline.py</files>
  <action>Update rank_from_query_embedding() to call disambiguate_title_collisions() after semantic_primary_sort() and before final top-5 clipping (around line 335). Pass reranked candidates, evidence, metadata_by_pubkey, and config. Ensure integration is conditional on evidence being available and does not affect queries without duplicate titles. Add pipeline integration tests verifying: (a) disambiguation only activates when collisions exist, (b) non-collision queries preserve exact Phase 2 ordering, and (c) collision queries apply signal-based reordering within collision groups only.</action>
  <acceptance_criteria>Pipeline integration demonstrates disambiguation refines semantic reranking without regressing non-collision cases.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_pipeline.py tests/test_disambiguation.py -x</automated>
  </verify>
  <done>RNK-03 integration is active and Phase 2 behaviors remain intact.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_disambiguation.py tests/test_pipeline.py -x`
- `uv run pytest -x`
</verification>

<success_criteria>
- Collision detection only activates when duplicate normalized titles exist in top-K results.
- Author signals have highest disambiguation weight, consistent with Phase 1 ordering.
- Semantic reranker order is preserved when disambiguation signals tie.
- No regression in non-collision query ordering (Phase 2 behaviors intact).
</success_criteria>

<output>
After completion, create `.planning/phases/03-duplicate-disambiguation/03-duplicate-disambiguation-01-SUMMARY.md`
</output>
