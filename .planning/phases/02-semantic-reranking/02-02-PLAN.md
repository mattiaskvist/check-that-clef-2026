---
phase: 02-semantic-reranking
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - main.py
  - tests/test_stage_diagnostics.py
  - tests/test_language_metrics_reporting.py
  - tests/test_cost_transparency.py
autonomous: true
requirements:
  - EVAL-03
  - RNK-02
must_haves:
  truths:
    - "Evaluation output distinguishes retrieval-stage misses from reranker ordering quality via stage diagnostics."
    - "Evaluation reports semantic reranked MRR@5 and uplift against baseline overall and per language (en/de/fr)."
    - "Evaluation reports reranker latency and throughput so quality/cost tradeoffs are visible."
  artifacts:
    - path: "main.py"
      provides: "Stage-level diagnostics and semantic uplift reporting in evaluate flow"
    - path: "tests/test_stage_diagnostics.py"
      provides: "Recall@K/uplift/latency diagnostic contract coverage"
    - path: "tests/test_language_metrics_reporting.py"
      provides: "Per-language uplift and no-language-regression compatibility checks"
  key_links:
    - from: "main.py::_evaluate"
      to: "scorer.py"
      via: "baseline vs semantic reranked MRR@5 computation"
      pattern: "MRR@5|uplift|scorer\\("
    - from: "main.py::_evaluate"
      to: "src/clef_retrieval/pipeline.py"
      via: "diagnostics derived from dense candidates and reranked outputs"
      pattern: "recall@|latency|throughput"
---

<objective>
Add stage diagnostics and uplift reporting for semantic reranking evaluation.

Purpose: Make Phase 2 results actionable by showing whether failures come from candidate recall or reranker ordering, while preserving Phase 1 multilingual promotion semantics.
Output: Evaluate diagnostics for Recall@K, semantic MRR@5 uplift (overall + per-language), and reranker latency/throughput with regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-reranking/02-CONTEXT.md
@.planning/phases/02-semantic-reranking/02-RESEARCH.md
@.planning/phases/02-semantic-reranking/02-VALIDATION.md
@.planning/phases/02-semantic-reranking/02-01-PLAN.md
@main.py
@tests/test_language_metrics_reporting.py
@tests/test_cost_transparency.py

<interfaces>
From main.py:
```python
def _evaluate(args: argparse.Namespace) -> int
def _predict_rows(...) -> list[dict[str, object]] | tuple[list[dict[str, object]], dict[str, int]]
```

Expected diagnostics outputs:
```text
Recall@50 (dense stage)
MRR@5 (semantic reranked)
Uplift delta vs baseline (overall + en/de/fr)
Reranker latency/throughput
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing diagnostics tests for Recall@K, semantic uplift, and reranker performance metrics (D-07, D-08, D-09)</name>
  <read_first>
    - .planning/phases/02-semantic-reranking/02-CONTEXT.md
    - .planning/phases/02-semantic-reranking/02-VALIDATION.md
    - tests/test_language_metrics_reporting.py
  </read_first>
  <files>tests/test_stage_diagnostics.py, tests/test_language_metrics_reporting.py, tests/test_cost_transparency.py</files>
  <action>Create failing tests that require evaluate output to include dense Recall@K diagnostics (default Recall@50), semantic reranked MRR@5, uplift delta against baseline overall and per-language (en/de/fr), and reranker latency/throughput lines. Include compatibility checks that subset-first/no-language-regression promotion messaging remains unchanged from Phase 1 semantics while adding new diagnostics fields.</action>
  <acceptance_criteria>Diagnostics tests fail on current evaluate output due to missing Recall@K/uplift/perf metrics, proving EVAL-03 gap coverage.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_stage_diagnostics.py tests/test_language_metrics_reporting.py</automated>
  </verify>
  <done>Evaluation diagnostics contract is explicit and test-driven before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evaluate-stage diagnostics and semantic uplift reporting in CLI (D-07, D-08, D-09)</name>
  <read_first>
    - main.py
    - scorer.py
    - src/clef_retrieval/query_policy.py
  </read_first>
  <files>main.py</files>
  <action>Extend `_evaluate` to compute and print: (a) dense-stage Recall@K diagnostics from candidate coverage, (b) semantic reranked MRR@5, (c) uplift delta versus baseline metrics overall and by language, and (d) reranker latency/throughput stats for the evaluated run. Keep output deterministic and parseable for tests. Preserve existing multilingual promotion gate behavior and subset-first semantics from Phase 1 without changing gate decisions.</action>
  <acceptance_criteria>Evaluate output includes all required diagnostics and continues to support Phase 1 promotion/no-regression flow.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_stage_diagnostics.py tests/test_language_metrics_reporting.py tests/test_cost_transparency.py</automated>
  </verify>
  <done>EVAL-03 diagnostics are available in standard evaluate runs and test-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Harden diagnostics defaults and CLI ergonomics for repeatable experiment loops</name>
  <read_first>
    - main.py
    - .planning/phases/02-semantic-reranking/02-RESEARCH.md
  </read_first>
  <files>main.py, tests/test_stage_diagnostics.py</files>
  <action>Add or refine CLI defaults/options needed for repeatable diagnostics (e.g., explicit rerank depth readout, stable Recall@K default tied to `rerank_top_k`, and latency/throughput formatting). Ensure diagnostics do not require full recompute when cached predictions are used and remain clear in subset runs. Note in code comments where diagnostics address review concern: bottleneck localization ambiguity.</action>
  <acceptance_criteria>Diagnostics are consistently emitted in both cached and recompute evaluate paths with stable labels for automated tests.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_stage_diagnostics.py tests/test_cost_transparency.py</automated>
  </verify>
  <done>Semantic evaluation telemetry is practical for fast iteration and comparable across runs.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q tests/test_stage_diagnostics.py tests/test_language_metrics_reporting.py tests/test_cost_transparency.py`
- `uv run pytest -q`
</verification>

<success_criteria>
- Evaluate command reports Recall@K, semantic MRR@5 uplift overall/per-language, and reranker latency/throughput.
- Output clearly separates retrieval bottlenecks from reranker bottlenecks.
- Phase 1 subset-first and no-language-regression semantics remain compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-reranking/02-semantic-reranking-02-SUMMARY.md`
</output>

