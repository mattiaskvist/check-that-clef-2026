---
phase: 02-semantic-reranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clef_retrieval/config.py
  - src/clef_retrieval/reranker.py
  - src/clef_retrieval/pipeline.py
  - tests/test_reranker_backends.py
  - tests/test_pipeline_semantic_rerank.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - RNK-01
  - RNK-02
must_haves:
  truths:
    - "System can rerank dense candidates with semantic cross-encoder scoring instead of lexical overlap."
    - "Reranker backend is configurable between Jina v2 and BGE v2-m3, with Jina as default."
    - "Top-50 rerank depth is configurable and semantic score is primary ordering while Phase 1 weighted score is tie-break only."
  artifacts:
    - path: "src/clef_retrieval/config.py"
      provides: "Semantic reranker backend and rerank depth configuration"
    - path: "src/clef_retrieval/reranker.py"
      provides: "Backend adapter protocol and scoring/tie-break primitives"
    - path: "src/clef_retrieval/pipeline.py"
      provides: "Dense retrieve -> semantic rerank integration path"
    - path: "tests/test_reranker_backends.py"
      provides: "Config/backend default and adapter contract regression tests"
    - path: "tests/test_pipeline_semantic_rerank.py"
      provides: "Semantic-primary ordering and deterministic tie-break tests"
  key_links:
    - from: "src/clef_retrieval/pipeline.py"
      to: "src/clef_retrieval/reranker.py"
      via: "semantic reranker call on top rerank_top_k dense candidates"
      pattern: "rerank_top_k|score_candidates|semantic"
    - from: "src/clef_retrieval/config.py"
      to: "src/clef_retrieval/reranker.py"
      via: "backend selection and model id config"
      pattern: "reranker_backend|jina|bge"
---

<objective>
Implement semantic reranking in the retrieval pipeline with backend-swappable adapters and deterministic tie-break semantics.

Purpose: Satisfy Phase 2 semantic-first ranking behavior while preserving multilingual and deterministic experiment constraints from Phase 1.
Output: Config fields, reranker adapters, pipeline integration, and failing-first tests converted to passing.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-semantic-reranking/02-CONTEXT.md
@.planning/phases/02-semantic-reranking/02-RESEARCH.md
@src/clef_retrieval/config.py
@src/clef_retrieval/pipeline.py
@src/clef_retrieval/reranker.py
@src/clef_retrieval/retriever.py
@tests/test_pipeline.py
@tests/test_signal_weighting.py

<interfaces>
From src/clef_retrieval/config.py:
```python
class RetrievalConfig(BaseModel):
    top_k: int
    top_n: int
    weight_title_author: float
    weight_method_finding: float
    weight_keywords: float
```

From src/clef_retrieval/pipeline.py:
```python
def weighted_signal_rerank(...)-> list[str]
def rank_from_query_embedding(...)-> list[str]
```

From src/clef_retrieval/reranker.py:
```python
class RerankResult(BaseModel):
    ranked_pubkeys: list[str]
    scores: list[float]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing semantic reranker contract tests (D-01, D-02, D-03, D-05, D-06)</name>
  <read_first>
    - .planning/phases/02-semantic-reranking/02-CONTEXT.md
    - src/clef_retrieval/pipeline.py
    - src/clef_retrieval/reranker.py
  </read_first>
  <files>tests/test_reranker_backends.py, tests/test_pipeline_semantic_rerank.py, tests/test_pipeline.py</files>
  <action>Create failing tests that lock: (a) config supports `jina_v2` and `bge_v2_m3` with `jina_v2` default per D-01/D-02, (b) pipeline reranks only top `rerank_top_k` dense candidates with default 50 per D-03, (c) semantic score is primary sort key while Phase 1 weighted score only breaks semantic ties per D-05/D-06, and (d) deterministic ordering fallback preserves candidate order when both semantic and weighted signals tie.</action>
  <acceptance_criteria>New tests fail on current lexical-first pipeline and missing backend config, proving coverage for RNK-01/RNK-02 behavior.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_reranker_backends.py tests/test_pipeline_semantic_rerank.py</automated>
  </verify>
  <done>Phase-2 semantic contracts are explicit and executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reranker backend adapters and config controls (D-01, D-02, D-03, D-04)</name>
  <read_first>
    - src/clef_retrieval/config.py
    - src/clef_retrieval/reranker.py
    - .planning/phases/02-semantic-reranking/02-RESEARCH.md
  </read_first>
  <files>src/clef_retrieval/config.py, src/clef_retrieval/reranker.py</files>
  <action>Add RetrievalConfig fields for `reranker_backend` (`jina_v2|bge_v2_m3`, default `jina_v2` per D-02), `rerank_top_k` (default 50 per D-03), backend model IDs, and tie epsilon. In `reranker.py`, define backend-agnostic semantic scoring interface and adapters for Jina v2 and BGE v2-m3; include clear error messaging if optional backend dependencies are unavailable. Keep adapter API rank-order focused (no blended score normalization) and deterministic.</action>
  <acceptance_criteria>Config and reranker modules expose backend selection and score computation contracts needed by pipeline integration with deterministic behavior.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_reranker_backends.py tests/test_config.py</automated>
  </verify>
  <done>Semantic reranker backends are selectable from config and default to Jina without changing call sites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate semantic-primary reranking into pipeline with weighted tie-break only (D-05, D-06)</name>
  <read_first>
    - src/clef_retrieval/pipeline.py
    - src/clef_retrieval/retriever.py
    - tests/test_signal_weighting.py
  </read_first>
  <files>src/clef_retrieval/pipeline.py, tests/test_pipeline_semantic_rerank.py, tests/test_pipeline.py</files>
  <action>Update `rank_from_query_embedding()` to retrieve dense candidates first, rerank only top `rerank_top_k`, then produce final ordering with semantic score descending as primary. Reuse Phase 1 weighted scoring only when semantic scores are equal within epsilon (tie-break path only), and retain deterministic stable fallback to original dense rank. Keep existing subset-first and no-language-regression semantics compatible by not changing `query_policy` promotion helpers and by preserving output top-5 contract.</action>
  <acceptance_criteria>Pipeline output demonstrates semantic-first ordering with deterministic tie behavior and no regression in existing top-5 output shape contracts.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_pipeline_semantic_rerank.py tests/test_pipeline.py tests/test_signal_weighting.py</automated>
  </verify>
  <done>RNK-01 semantic reranking path is active and RNK-02 deterministic multilingual-safe tie semantics remain intact.</done>
</task>

</tasks>

<verification>
- `uv run pytest -q tests/test_reranker_backends.py tests/test_pipeline_semantic_rerank.py tests/test_pipeline.py tests/test_signal_weighting.py`
- `uv run pytest -q`
</verification>

<success_criteria>
- Backend-selectable semantic reranker is implemented with Jina default and BGE compatibility.
- Dense retrieval candidate handoff uses configurable rerank depth with default 50.
- Semantic score drives ordering; Phase 1 weighted signal affects only ties.
- Existing pipeline contracts (top-5 shape, deterministic behavior) stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-semantic-reranking/02-semantic-reranking-01-SUMMARY.md`
</output>

