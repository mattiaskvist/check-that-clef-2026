---
phase: 01-query-understanding
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - main.py
  - tests/test_cli.py
  - tests/test_language_metrics_reporting.py
  - tests/test_cost_transparency.py
autonomous: true
requirements:
  - QRY-02
  - QRY-04
  - EVAL-01
  - EVAL-02
  - OPS-01
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "CLI reports extraction accepted/rejected/fallback outcomes for each run."
    - "Evaluation output includes overall and per-language MRR@5 metrics."
    - "Subset-first seeded experiments can be run and promoted only with no language regression."
    - "Cached evaluation is reused unless recompute is explicitly requested."
    - "CLI makes API/model usage and cost guardrail status visible."
  artifacts:
    - path: "main.py"
      provides: "Seeded subset controls, telemetry output, per-language reporting, and cost guardrails"
    - path: "tests/test_cli.py"
      provides: "Regression coverage for CLI flags and cached-evaluation path"
    - path: "tests/test_language_metrics_reporting.py"
      provides: "Per-language metrics and promotion gate behavior checks"
    - path: "tests/test_cost_transparency.py"
      provides: "CLI cost transparency and guardrail warning checks"
  key_links:
    - from: "main.py::_predict_rows"
      to: "src/clef_retrieval/pipeline.py"
      via: "extraction outcome metadata from query construction"
      pattern: "parsed\\+accepted|parsed\\+rejected|error->fallback"
    - from: "main.py::_evaluate"
      to: "scorer.py"
      via: "overall + per-language scoring path"
      pattern: "scorer\\("
---

<objective>
Operationalize query-understanding experiments with telemetry, reproducible subset loops, multilingual metrics, and cost safeguards.

Purpose: Make Phase 1 verifiable and repeatable in CLI runs, including promotion gate enforcement and cached evaluation behavior.
Output: Updated CLI flows and tests for metrics, transparency, and operational controls.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-query-understanding/01-CONTEXT.md
@.planning/phases/01-query-understanding/01-RESEARCH.md
@.planning/phases/01-query-understanding/01-01-PLAN.md
@.planning/phases/01-query-understanding/01-02-PLAN.md
@main.py
@tests/test_cli.py
@scorer.py

<interfaces>
From main.py:
```python
def _predict(args: argparse.Namespace) -> int
def _evaluate(args: argparse.Namespace) -> int
def build_parser() -> argparse.ArgumentParser
```

From pipeline metadata helper (added in Plan 01-02):
```python
def build_query_embedding_text_with_outcome(tweet_text: str, service: GeminiService) -> tuple[str, str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI tests for multilingual metrics, subset-seed reproducibility, promotion gate, and cost transparency</name>
  <read_first>
    - tests/test_cli.py
    - .planning/phases/01-query-understanding/01-RESEARCH.md
  </read_first>
  <files>tests/test_language_metrics_reporting.py, tests/test_cost_transparency.py, tests/test_cli.py</files>
  <action>Create failing tests that validate: per-language MRR output lines for en/de/fr (QRY-04, EVAL-02), reuse of cached predictions unless `--recompute` (EVAL-01), seeded subset reproducibility flags and deterministic sample identity (D-05, D-06, OPS-01), promotion gate rejects any language regression (D-07), and CLI emits extraction-outcome + estimated API usage/cost warnings when thresholds are exceeded (QRY-02, OPS-02, OPS-03).</action>
  <acceptance_criteria>Tests fail before implementation for newly introduced CLI capabilities and guardrails.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_cli.py tests/test_language_metrics_reporting.py tests/test_cost_transparency.py</automated>
  </verify>
  <done>Executable tests define complete Phase-1 CLI behavior and promotion policy checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI seeded subset loop, extraction telemetry, and promotion/no-regression gate</name>
  <read_first>
    - main.py
    - src/clef_retrieval/query_policy.py
  </read_first>
  <files>main.py</files>
  <action>Add CLI arguments and logic for subset-first experimentation with fixed seed (`--subset-seed`, per-language limit, mode marker), and enforce D-07 promotion gate in evaluation output. Integrate extraction outcome categories from pipeline helper and print accepted/rejected/fallback counts and rates (QRY-02). Ensure language-aware reporting includes per-language MRR@5 alongside overall values (QRY-04, EVAL-02), while keeping existing cached evaluation behavior intact (EVAL-01).</action>
  <acceptance_criteria>CLI can run reproducible subset experiments and clearly decide promote/block based on no-regression rule; telemetry and metrics are visible in stdout.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_cli.py tests/test_language_metrics_reporting.py</automated>
  </verify>
  <done>Main CLI behavior satisfies subset-first workflow with explicit multilingual promotion decisions and extraction observability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement model/API usage transparency and practical cost guardrails</name>
  <read_first>
    - main.py
    - .planning/PROJECT.md
  </read_first>
  <files>main.py, tests/test_cost_transparency.py</files>
  <action>Add deterministic estimation and reporting for query-time model/API usage (calls per query, total projected calls, extraction enabled/disabled mode) and add practical budget guardrails with warning/abort behavior unless explicitly overridden (OPS-03). Keep defaults conservative to support iterative experiments and align with cost-awareness constraints in project context.</action>
  <acceptance_criteria>CLI output includes transparent usage estimates and explicit guardrail status, with override flag behavior covered by tests.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_cost_transparency.py tests/test_cli.py</automated>
  </verify>
  <done>Operators can see and control likely API spend before expensive full runs.</done>
</task>

</tasks>

<verification>
Run full Phase 1 CLI verification suite:
- `uv run pytest -q tests/test_cli.py tests/test_language_metrics_reporting.py tests/test_cost_transparency.py`
- `uv run pytest -q`
</verification>

<success_criteria>
- Per-language and overall MRR reporting is automated and reproducible.
- Subset-first seeded experiments and no-regression promotion gate are enforced in CLI flow.
- Cached evaluation path remains available and tested.
- Extraction telemetry and cost transparency are visible and actionable in every run.
</success_criteria>

<output>
After completion, create `.planning/phases/01-query-understanding/01-query-understanding-03-SUMMARY.md`
</output>

