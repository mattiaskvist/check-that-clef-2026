---
phase: 01-query-understanding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clef_retrieval/config.py
  - src/clef_retrieval/query_policy.py
  - tests/test_query_policy.py
  - tests/test_subset_policy.py
autonomous: true
requirements:
  - QRY-01
  - QRY-04
  - OPS-01
must_haves:
  truths:
    - "Structured extraction is only considered usable when strict gate checks pass."
    - "Language-aware normalization logic exists for en/de/fr before scoring."
    - "Subset selection can be reproduced exactly with a fixed seed."
  artifacts:
    - path: "src/clef_retrieval/query_policy.py"
      provides: "Strict gate, normalization, seeded subset, and promotion-policy helpers"
    - path: "src/clef_retrieval/config.py"
      provides: "Config knobs for gate thresholds, weighting order, and subset seed policy"
    - path: "tests/test_query_policy.py"
      provides: "Automated checks for gate and normalization behavior"
    - path: "tests/test_subset_policy.py"
      provides: "Automated checks for deterministic subset behavior"
  key_links:
    - from: "main.py"
      to: "src/clef_retrieval/query_policy.py"
      via: "subset and promotion helper imports"
      pattern: "from clef_retrieval\\.query_policy import"
    - from: "src/clef_retrieval/pipeline.py"
      to: "src/clef_retrieval/query_policy.py"
      via: "gate + normalization helper calls"
      pattern: "query_policy"
---

<objective>
Establish query-understanding policy contracts and deterministic experiment controls that all later implementation plans consume.

Purpose: Lock strict gate + language normalization + seeded subset behavior up front so downstream implementation follows user decisions exactly.
Output: A new policy module, config fields, and deterministic policy tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-query-understanding/01-CONTEXT.md
@.planning/phases/01-query-understanding/01-RESEARCH.md
@src/clef_retrieval/config.py
@src/clef_retrieval/pipeline.py
@src/clef_retrieval/schemas.py

<interfaces>
From src/clef_retrieval/config.py:
```python
class RetrievalConfig(BaseModel):
    query_model: str
    query_batch_size: int
```

From src/clef_retrieval/schemas.py:
```python
class TweetEvidence(BaseModel):
    language: str
    candidate_title_mentions: list[str]
    candidate_authors: list[str]
    keywords: list[str]
    method_terms: list[str]
    finding_terms: list[str]
    negative_constraints: list[str]
    query_text_for_embedding: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict policy config and ordering constants</name>
  <read_first>
    - src/clef_retrieval/config.py
    - .planning/phases/01-query-understanding/01-CONTEXT.md
  </read_first>
  <files>src/clef_retrieval/config.py</files>
  <action>Add query-understanding config fields to RetrievalConfig for strict gate thresholds and deterministic weighting constants. Preserve D-03 ordering explicitly in defaults (title+author weight > method/finding weight > keyword weight) and add language normalization mode for en/de/fr per D-04. Add subset policy defaults (`subset_seed`, `subset_per_language_limit`) per D-05 and D-06.</action>
  <acceptance_criteria>Config exposes explicit gate/weight/subset fields with validated ranges and defaults matching D-02, D-03, D-04, D-05, and D-06.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_config.py</automated>
  </verify>
  <done>RetrievalConfig contains all policy controls used by later plans with no hardcoded magic constants left outside config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create query policy module for gate, normalization, subset, and promotion contracts</name>
  <read_first>
    - src/clef_retrieval/schemas.py
    - .planning/phases/01-query-understanding/01-RESEARCH.md
  </read_first>
  <files>src/clef_retrieval/query_policy.py</files>
  <action>Create `query_policy.py` with typed helpers: (1) strict extraction gate evaluator requiring parsed evidence plus non-empty key fields per D-02, (2) language-aware token normalization helper for en/de/fr per D-04, (3) deterministic subset selector with seed input per D-05 and D-06, and (4) promotion decision helper enforcing “improves on subset and no language regression” per D-07. Keep this module pure/deterministic so main and pipeline can consume it directly.</action>
  <acceptance_criteria>All four helpers are exported and deterministic; no network or model calls are embedded in policy logic.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_query_policy.py tests/test_subset_policy.py</automated>
  </verify>
  <done>Policy module exists and is importable by pipeline/main with stable function signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add policy-focused tests for strict gate and seeded subset reproducibility</name>
  <read_first>
    - tests/test_pipeline.py
    - tests/test_cli.py
  </read_first>
  <files>tests/test_query_policy.py, tests/test_subset_policy.py</files>
  <action>Write tests that fail before implementation and pass after: strict gate rejects parsed-but-weak evidence, language normalization applies per-language behavior for en/de/fr, subset selection is reproducible with same seed and differs with different seeds, and promotion helper rejects any language regression even if macro average improves (D-07).</action>
  <acceptance_criteria>Tests encode locked decisions D-02 through D-07 directly, not inferred indirectly from implementation details.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_query_policy.py tests/test_subset_policy.py</automated>
  </verify>
  <done>Policy tests prevent future regressions on strict gate, normalization, seeded subsets, and promotion gate semantics.</done>
</task>

</tasks>

<verification>
Run policy and config test suite:
- `uv run pytest -q tests/test_config.py tests/test_query_policy.py tests/test_subset_policy.py`
</verification>

<success_criteria>
- Strict extraction gate contract exists and is test-verified.
- Weight-order and normalization policies are represented in config and code (D-03, D-04).
- Seeded subset and no-regression promotion helpers are deterministic and test-verified (D-05, D-06, D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/01-query-understanding/01-query-understanding-01-SUMMARY.md`
</output>

