---
phase: 01-query-understanding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/clef_retrieval/pipeline.py
  - tests/test_pipeline.py
  - tests/test_signal_weighting.py
autonomous: true
requirements:
  - QRY-01
  - QRY-02
  - QRY-03
  - QRY-05
must_haves:
  truths:
    - "Structured query signals are used only when strict gate passes; otherwise raw tweet fallback is used."
    - "Ranking prioritizes title+author matches above method/finding above generic keywords."
    - "Negative constraints from tweets reduce obvious false-positive candidates before final top-5."
  artifacts:
    - path: "src/clef_retrieval/pipeline.py"
      provides: "Gate-aware query construction and weighted ranking integration"
    - path: "tests/test_pipeline.py"
      provides: "Regression checks for fallback safety and top-5 behavior"
    - path: "tests/test_signal_weighting.py"
      provides: "Ordering and negative-constraint scoring checks"
  key_links:
    - from: "src/clef_retrieval/pipeline.py"
      to: "src/clef_retrieval/query_policy.py"
      via: "strict gate and normalization helper calls"
      pattern: "from \\.query_policy import"
    - from: "src/clef_retrieval/pipeline.py"
      to: "src/clef_retrieval/schemas.py"
      via: "TweetEvidence scoring inputs"
      pattern: "TweetEvidence"
---

<objective>
Implement gate-aware query handling and signal-priority reranking in the retrieval pipeline.

Purpose: Convert policy contracts into actual ranking behavior so query-understanding improvements affect retrieval quality.
Output: Updated pipeline behavior plus focused signal-order and constraint tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-query-understanding/01-CONTEXT.md
@.planning/phases/01-query-understanding/01-RESEARCH.md
@.planning/phases/01-query-understanding/01-01-PLAN.md
@src/clef_retrieval/pipeline.py
@src/clef_retrieval/query_policy.py
@src/clef_retrieval/schemas.py

<interfaces>
From src/clef_retrieval/pipeline.py:
```python
def build_query_embedding_text(tweet_text: str, service: GeminiService) -> str
def stage2_rerank(tweet_text: str, candidates: Sequence[str], rerank_fn: Callable[[str, Sequence[str]], Sequence[str]] | None = None) -> list[str]
```

From src/clef_retrieval/query_policy.py:
```python
def extraction_gate_passes(evidence: TweetEvidence, *, min_title_mentions: int, min_candidate_authors: int) -> bool
def normalize_terms(values: list[str], language: str) -> list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for gated extraction, signal weighting order, and negative constraints</name>
  <read_first>
    - tests/test_pipeline.py
    - src/clef_retrieval/pipeline.py
  </read_first>
  <files>tests/test_pipeline.py, tests/test_signal_weighting.py</files>
  <action>Write explicit failing tests that enforce D-01/D-02 fallback behavior, D-03 signal ordering (title+author outranks method/finding, which outranks keywords), and QRY-05 negative-constraint effect on ranking. Include a parsed-but-weak evidence case that must fallback to raw tweet and be counted as rejected later.</action>
  <acceptance_criteria>Tests fail against current pipeline because weighted signal ranking and strict gate logic are not yet fully implemented.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_pipeline.py tests/test_signal_weighting.py</automated>
  </verify>
  <done>Test suite defines target behaviors unambiguously before code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict gate-aware query text construction and weighted reranking</name>
  <read_first>
    - src/clef_retrieval/query_policy.py
    - src/clef_retrieval/config.py
  </read_first>
  <files>src/clef_retrieval/pipeline.py</files>
  <action>Update pipeline to use structured Gemini extraction as default per D-01, but only consume structured fields when strict gate passes per D-02. Implement deterministic weighted reranking using normalized title/authors, method/finding terms, and keywords in D-03 order with language-aware normalization per D-04. Apply negative constraints as explicit penalty or exclusion in rerank scoring so prohibited cues lower rank per QRY-05.</action>
  <acceptance_criteria>Pipeline exposes deterministic rerank behavior where signal priorities are encoded in code, and rejected extraction path cleanly falls back to raw tweet text.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_pipeline.py tests/test_signal_weighting.py</automated>
  </verify>
  <done>All new pipeline tests pass and current top-5 shape guarantees remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve backward-compatible pipeline interfaces and add extraction outcome metadata hook</name>
  <read_first>
    - src/clef_retrieval/pipeline.py
    - main.py
  </read_first>
  <files>src/clef_retrieval/pipeline.py, tests/test_pipeline.py</files>
  <action>Keep existing `build_query_embedding_text` callable for compatibility while adding a companion helper that returns extraction outcome category (`parsed+accepted`, `parsed+rejected`, `error->fallback`) for QRY-02 telemetry in later plans. Do not break existing predict/evaluate callers.</action>
  <acceptance_criteria>Legacy call sites continue to work, and a metadata-capable helper is available for CLI instrumentation work.</acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_pipeline.py</automated>
  </verify>
  <done>Pipeline offers both compatibility path and outcome-metadata path with tests validating output contract.</done>
</task>

</tasks>

<verification>
Run targeted and full pipeline tests:
- `uv run pytest -q tests/test_pipeline.py tests/test_signal_weighting.py`
- `uv run pytest -q tests/test_pipeline.py tests/test_cli.py`
</verification>

<success_criteria>
- Query construction follows D-01 and D-02 with deterministic fallback behavior.
- Weighted rerank implements D-03 ordering with D-04 normalization.
- Negative constraints are enforced before final top-5 output.
- Extraction outcomes are classifiable for telemetry (QRY-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-query-understanding/01-query-understanding-02-SUMMARY.md`
</output>

